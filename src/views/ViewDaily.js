import React from "react";
import styles from "../css/ViewDaily.module.scss";
import { PropTypes } from "prop-types";

const ViewDaily = () => {
  return (
    <div className={styles.ViewDaily}>
      {/*  */}
      {/*  */}
    </div>
  );
};

export default ViewDaily;

ViewDaily.defaultProps = {};

ViewDaily.propTypes = {};
